on:
  workflow_dispatch:
  push:
     branches:
       - master

jobs:
  run-export-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # required for accessing files in repository.

      - name: Export Part I
        run: bash ${PWD}/export.sh 1
      - name: Export Part II
        run: bash ${PWD}/export.sh 2
      - name: Export Part III
        run: bash ${PWD}/export.sh 3
